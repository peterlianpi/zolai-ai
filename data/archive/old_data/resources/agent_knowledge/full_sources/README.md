# Full sources (verbatim copies)

This folder contains **verbatim Markdown copies** of selected reference texts so you can:

- search them easily inside the repo
- quote exact wording when deciding standardization rules
- use them as long-form reference during dataset fixes

These are not “distilled” notes — they are the full text content, preserved as-is.

