# Brand assets (Zolai AI)

- `zolai-logo.svg` — avatar source (render 500×500 → `logo.png`)
- `zolai-og.svg` — social card source (render 1200×630 → `og.png`)
- Render with ImageMagick: `convert -background none zolai-logo.svg -resize 500x500 logo.png`
- Deployed to `Zolai-AI/.github` (org profile + avatar manual upload).
