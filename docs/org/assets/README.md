# Brand assets (Zolai AI)

- Org avatar + social card use the **existing website brand**:
  `website/zolai-project/public/logo.png` (avatar) and `og.png` (social card).
- Deployed to `Zolai-AI/.github` (logo.png, og.png).
- Org avatar upload is owner-only: Settings -> Profile -> Upload new picture.
