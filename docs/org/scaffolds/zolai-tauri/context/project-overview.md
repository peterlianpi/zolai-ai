# Zolai Tauri Desktop

Bundled offline desktop app (Rust/Tauri). Serves the Zolai Core FastAPI server
locally and runs a local GGUF model via Ollama for fully offline use.

## Goals
1. Bundle core server + local model for offline translation/chat.
2. Simple native UI shell over the web platform.
3. Cross-platform builds via GitHub Actions.
